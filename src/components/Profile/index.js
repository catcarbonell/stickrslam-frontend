import React from 'react';
import PropTypes from 'prop-types';
import './Profile.css';

const Profile = props => {
    return(
        <div className="Profile">
            <h1>Profile</h1>
        </div>
    )
}

Profile.propTypes = {};
export default Profile;